# Gemini API Key (REQUIRED for AI Chat Widget)
# Get your key from: https://aistudio.google.com/app/apikey
# NEVER commit .env.local to git!
GEMINI_API_KEY=your_api_key_here

# Gemini Model (OPTIONAL - defaults to gemini-1.5-flash)
# If the default model is unavailable, the system will auto-discover and select the best available model
# GEMINI_MODEL=gemini-1.5-flash

# EmailJS Configuration (optional - already configured in code)
# VITE_EMAILJS_SERVICE_ID=your_service_id
# VITE_EMAILJS_TEMPLATE_ID=your_template_id
# VITE_EMAILJS_PUBLIC_KEY=your_public_key
